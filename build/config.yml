name: "shotgun_code"
description: "A modern code generation tool"
author:
  name: "WhiteBite"
  email: ""

frontend:
  dir: "./frontend"
  install: "npm install"
  build: "npm run build"
  dev: "npm run dev"
  package: "./dist"

backend:
  dir: "./backend"
  mod: "./backend/go.mod"
  build: "go build -o ../bin/"

build:
  outputdir: "./bin"