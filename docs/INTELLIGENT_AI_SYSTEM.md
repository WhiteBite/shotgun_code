# 🤖 Интеллектуальная система ИИ

## Обзор

Интеллектуальная система ИИ для проекта Shotgun Code предоставляет расширенные возможности для работы с искусственным интеллектом, включая автоматическую оптимизацию, умный выбор моделей, fallback систему и анализ качества результатов.

## 🚀 Ключевые возможности

### 🔥 Автоматическая оптимизация
- **Оптимизация промптов**: Система автоматически улучшает пользовательские промпты для лучших результатов
- **Сжатие контекста**: Умное сжатие контекста для экономии токенов
- **Оптимизация токенов**: Автоматический выбор оптимального количества токенов

### 🧠 Интеллектуальный выбор модели
- **Стратегии выбора**:
  - `fastest` - Быстрее всего
  - `cheapest` - Дешевле всего  
  - `best` - Лучшее качество
  - `balanced` - Сбалансированно

### 🔄 Fallback система
- **Автоматический переход** на резервные провайдеры при ошибках
- **Повторные попытки** с экспоненциальной задержкой
- **Сохранение контекста** при переключении между провайдерами

### 📊 Анализ и мониторинг
- **Оценка качества** результатов
- **Мониторинг производительности** провайдеров
- **Статистика использования** токенов
- **Анализ успешности** запросов

## 🏗️ Архитектура

### Backend компоненты

#### `backend/domain/models_ai.go`
Расширенные модели для работы с ИИ:
```go
type AIRequest struct {
    Model        string
    SystemPrompt string
    UserPrompt   string
    Temperature  float64
    MaxTokens    int
    TopP         float64
    Priority     RequestPriority
    // ... дополнительные поля
}

type IntelligentRequest struct {
    BaseRequest    AIRequest
    Optimization   OptimizationConfig
    FallbackConfig FallbackConfig
    Monitoring     MonitoringConfig
}
```

#### `backend/domain/ai_provider.go`
Интерфейсы для интеллектуальных провайдеров:
```go
type IntelligentAIProvider interface {
    AIProvider
    GenerateIntelligent(ctx context.Context, req IntelligentRequest) (AIResponse, error)
    OptimizePrompt(ctx context.Context, originalPrompt string, context []string) (string, error)
    SelectOptimalModel(ctx context.Context, req AIRequest, strategy ModelSelectionStrategy) (string, error)
    // ... дополнительные методы
}
```

#### `backend/application/intelligent_ai_service.go`
Основной сервис интеллектуальной системы:
```go
type IntelligentAIService struct {
    settingsService *SettingsService
    log             domain.Logger
    providerFactory ProviderFactory
    providers       map[string]domain.AIProvider
    rateLimiter     *RateLimiter
    metrics         *MetricsCollector
}
```

### Frontend компоненты

#### `frontend/src/stores/intelligent-ai.store.ts`
Store для управления состоянием интеллектуальной системы:
```typescript
export const useIntelligentAIStore = defineStore("intelligentAI", () => {
    // Состояние
    const isLoading = ref(false);
    const lastResult = ref<IntelligentGenerationResult | null>(null);
    const currentOptions = ref<IntelligentGenerationOptions>({...});
    
    // Действия
    const generateIntelligentCode = async (task: string, context: string, options?: Partial<IntelligentGenerationOptions>) => {
        // Логика генерации
    };
    
    return { /* ... */ };
});
```

#### `frontend/src/components/workspace/IntelligentAIPanel.vue`
UI компонент для работы с интеллектуальной системой:
- Поле ввода задачи
- Настройки генерации
- Отображение результатов
- Информация о провайдере
- Список доступных моделей

## ⚙️ Настройки

### Основные параметры
```typescript
interface IntelligentGenerationOptions {
    temperature: number;           // 0.7 (сбалансированная креативность)
    maxTokens: number;            // 4000 (достаточно для большинства задач)
    topP: number;                 // 1.0 (полный выбор токенов)
    priority: "low" | "normal" | "high" | "critical";
    timeout: number;              // 60000ms (60 секунд)
    maxRetries: number;           // 3 (количество повторных попыток)
}
```

### Интеллектуальные возможности
```typescript
{
    autoOptimizePrompt: boolean;      // Автоматическая оптимизация промпта
    contextCompression: boolean;      // Сжатие контекста
    tokenOptimization: boolean;       // Оптимизация токенов
    modelSelectionStrategy: "fastest" | "cheapest" | "best" | "balanced";
    enableFallback: boolean;          // Включить fallback провайдеры
    fallbackProviders: string[];      // ["openai", "gemini"]
    maxFallbackAttempts: number;      // 2 (максимум попыток fallback)
}
```

## 🎯 Примеры использования

### Базовое использование
```typescript
// Генерация кода с интеллектуальными возможностями
const result = await intelligentAIStore.generateIntelligentCode(
    "Добавить валидацию формы регистрации",
    projectContext,
    {
        temperature: 0.7,
        priority: "normal",
        autoOptimizePrompt: true,
        enableFallback: true
    }
);
```

### Расширенное использование
```typescript
// Генерация с полным контролем параметров
const result = await intelligentAIStore.generateIntelligentCode(
    "Оптимизировать запрос к базе данных",
    projectContext,
    {
        temperature: 0.3,                    // Низкая креативность для точности
        maxTokens: 2000,                     // Ограниченное количество токенов
        priority: "high",                    // Высокий приоритет
        modelSelectionStrategy: "best",      // Лучшее качество
        projectType: "Go",                   // Специфичный для Go
        codeStyle: "clean code",             // Стиль кода
        enableFallback: true,                // Включить fallback
        fallbackProviders: ["openai", "gemini"]
    }
);
```

## 📊 Мониторинг и аналитика

### Метрики производительности
- **Время обработки** запросов
- **Количество токенов** использованных
- **Успешность** запросов
- **Производительность** провайдеров

### Анализ качества
- **Quality Score**: Оценка качества результата (0-1)
- **Relevance Score**: Релевантность ответа
- **Completeness Score**: Полнота ответа
- **Suggestions**: Предложения по улучшению

### Логирование
```go
// Логирование в IntelligentAIService
s.log.Info(fmt.Sprintf("Intelligent generation started for request %s", req.RequestID))
s.log.Warning(fmt.Sprintf("Fallback to provider %s", providerType))
s.log.Error(fmt.Sprintf("All generation attempts failed: %w", err))
```

## 🔄 Fallback система

### Принцип работы
1. **Основной провайдер** не отвечает или возвращает ошибку
2. **Автоматический переход** на резервный провайдер
3. **Сохранение контекста** и параметров запроса
4. **Повторные попытки** с экспоненциальной задержкой

### Конфигурация fallback
```go
fallbackProviders := []string{"openai", "gemini", "openrouter"}

for _, providerType := range fallbackProviders {
    if providerType == dto.SelectedProvider {
        continue // Пропускаем основной провайдер
    }
    
    provider, err := s.createProvider(providerType, dto)
    if err != nil {
        continue
    }
    
    response, err := provider.Generate(ctx, req)
    if err == nil {
        s.log.Info(fmt.Sprintf("Successfully used fallback provider %s", providerType))
        return response, nil
    }
}
```

## 🧪 Тестирование

### Unit тесты
```go
func TestIntelligentAIService_GenerateIntelligentCode(t *testing.T) {
    // Тестирование интеллектуальной генерации
}

func TestIntelligentAIService_Fallback(t *testing.T) {
    // Тестирование fallback системы
}

func TestIntelligentAIService_Optimization(t *testing.T) {
    // Тестирование оптимизации
}
```

### Интеграционные тесты
```go
func TestIntelligentAI_EndToEnd(t *testing.T) {
    // End-to-end тестирование всей системы
}
```

## 🔧 Интеграция

### В существующий UI
Интеллектуальная система полностью интегрирована в существующий интерфейс:

1. **Новый компонент** `IntelligentAIPanel.vue` добавлен в workspace
2. **Store** `intelligent-ai.store.ts` управляет состоянием
3. **API методы** добавлены в `api.service.ts`
4. **Обратная совместимость** с существующими функциями

### Расширение возможностей
Система легко расширяется для добавления новых возможностей:

```go
// Добавление нового провайдера
type NewProvider struct {
    // Реализация интерфейса AIProvider
}

// Добавление новых стратегий оптимизации
type CustomOptimizationStrategy struct {
    // Реализация стратегии
}
```

## 🚀 Производительность

### Оптимизации
- **Кэширование** провайдеров
- **Rate limiting** для предотвращения превышения лимитов
- **Асинхронная обработка** запросов
- **Сжатие контекста** для экономии токенов

### Мониторинг
- **Метрики в реальном времени**
- **Алерты** при проблемах с провайдерами
- **Статистика** использования ресурсов

## 📝 Логи и отладка

### Уровни логирования
```go
s.log.Debug("Detailed debug information")
s.log.Info("General information about system operation")
s.log.Warning("Warning messages for potentially harmful situations")
s.log.Error("Error messages for error conditions")
```

### Отладочная информация
- **Request ID** для отслеживания запросов
- **Provider information** для диагностики
- **Performance metrics** для оптимизации
- **Error details** для устранения проблем

## 🔮 Будущие улучшения

### Планируемые возможности
- **Машинное обучение** для улучшения оптимизации
- **Персонализация** настроек для пользователей
- **A/B тестирование** различных стратегий
- **Автоматическое обучение** на основе обратной связи

### Расширение провайдеров
- **Claude** (Anthropic)
- **Llama** (Meta)
- **Custom models** (локальные модели)

## 📚 Дополнительные ресурсы

- [Документация по API](API_DOCUMENTATION.md)
- [Руководство по настройке](SETUP_GUIDE.md)
- [Примеры использования](EXAMPLES.md)
- [Troubleshooting](TROUBLESHOOTING.md)

---

**Интеллектуальная система ИИ** готова к использованию и предоставляет мощные возможности для автоматизации разработки с помощью искусственного интеллекта.

