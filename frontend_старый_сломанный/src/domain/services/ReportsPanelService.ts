import { APP_CONFIG } from '@/config/app-config'

export class ReportsPanelService {
  constructor() {}

  // This service is intentionally left simple as ReportsPanel.vue has minimal logic
  // It's primarily a container component that delegates to other components
}