import { APP_CONFIG } from '@/config/app-config'

export class MainPanelService {
  // This service is intentionally left simple as MainPanel.vue has minimal logic
  // It's primarily a container component that delegates to other components
  
  constructor() {
    // No initialization needed
  }
}