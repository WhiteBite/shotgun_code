        id: T-999
        title: T-999 Final policy gate rehearsal
        description: "Прогон сквозных гейтов: build, tests, static, SBOM/licensing; политики."
        actions:
          - prompt: |
              Выполни изменения строго структурированными правками (Edits JSON / SAP).
              Уважай budgets и forbid-пути. Только внутри workspace.
              После применения: форматирование/импорты → build/type-check → (если нужно) tests/static.
            budgets: { maxFiles: 10, maxChangedLines: 300 }
        acceptance:
[{'kind': 'mcp.build.check', 'scope': 'all'}, {'kind': 'mcp.test.run', 'scope': 'all'}, {'kind': 'license.scan', 'scope': 'repo'}]
        onSuccess:
          - updateStatus: done
